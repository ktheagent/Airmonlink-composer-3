# Airmonlink Composer 3

This repository is the clean migration workspace for Airmonlink Composer.

**Current decision: RELEASE BLOCKED.**

The initial migration imports the proven notation and data engine from `ktheagent/Airmonlink-composer-2`, replaces the legacy runtime-injected interface with a native renderer shell, removes obsolete publishing injectors, and keeps Windows release builds disabled until every audit checkpoint passes.

No download should be recommended from this repository until the persistent release audit records PASS for source, regression, layout, docking, menus, publishing, performance, Windows runtime, installed application, artifacts, visual inspection, and adversarial self-critique.
