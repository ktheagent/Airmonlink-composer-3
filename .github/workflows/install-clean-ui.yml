name: Clean UI installer (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration retired
        run: |
          echo "This source-modifying migration workflow is retired."
          echo "Composer 3 source is committed directly and validated by validate-composer-3.yml."
